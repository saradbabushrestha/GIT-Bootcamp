export type ButtonComponentProps = {
  styles: string;
  btnText: string;
};

export type TextFieldComponentProps = {
  placeholder: string;
  name: string;
  inputType: string;
  icon?: React.ReactNode;
};
